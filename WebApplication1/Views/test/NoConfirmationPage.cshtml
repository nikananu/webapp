@{
    ViewData["Title"] = "NoConfirmationPage";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <style>
        /* Melancholy Styling */
        body {
            font-family: 'Georgia', serif;
            background: linear-gradient(to bottom right, #e0eaf3, #cfd8dc);
            color: #4a4a4a;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            background-color: rgba(255, 255, 255, 0.85);
            border-radius: 15px;
            padding: 30px;
            max-width: 500px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
        }

        h1 {
            font-size: 2.2em;
            color: #7a7a7a;
            margin: 0;
            animation: fadeIn 2s ease-in;
        }

        p {
            font-size: 1.1em;
            color: #4a4a4a;
            margin-top: 20px;
            line-height: 1.6;
        }

        .sad-face {
            font-size: 2em;
            color: #a6a6a6;
            margin: 15px 0;
            animation: pulse 2s infinite;
        }

        /* Fade-in effect */
        @@keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        /* Sad face pulse animation */
        @@keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Oh no, that's disappointing <span class="sad-face">😔</span></h1>
        <p>It’s okay; I understand. I hope we get a chance to connect another time. Until then, wishing you all the best!</p>
    </div>
</body>
</html>